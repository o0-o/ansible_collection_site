{# vim: ts=2:sw=2:sts=2:et:ft=yaml.ansible

Initial site inventory

#}
---
{{  ( ( "Inventory for "
        + site_uri
        + " (Site "
        + site['id'] | string
        + ")\n\n"
      )
      + default_comment_header | default()
      + "While this file is initially generated by the o0_o.site Ansible colleciton, it will not overwrite the whole file after it has been created. Changes should persist.

See README.md for documentation:

github.com/o0-o/ansible_collection_site/tree/main/roles/inventory"
    )
    | wordwrap(70)
    | comment(  prefix=yaml_comment_prefix | default(),
                postfix=default_comment_postfix | default() ) }}
all:
  children:
    {{ site_group }}:
      hosts:
